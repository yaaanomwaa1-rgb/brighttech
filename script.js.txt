// Welcome message
window.onload = function () {
    alert("Welcome to BrightTech!");
};
window.onload = function () {
    console.log("BrightTech Website Loaded!");
}

function sayHello() {
    alert("Thank you for visiting BrightTech!");
}
window.onload = function () {
    console.log("BrightTech Loaded Successfully!");
};

function welcomeUser(){
    alert("Welcome to BrightTech! We are happy to have you.");
}
window.onload = function () {
    console.log("BrightTech website is ready!");
};

document.addEventListener("submit", function(event) {
    event.preventDefault();
    alert("Thank you! Your message has been received.");
});
function darkMode(){

document.body.classList.toggle("dark");

}
let cart = 0;

function addToCart(){
    cart++;
    document.getElementById("cart-count").innerText = cart;
}

function checkout(){
    if(cart === 0){
        alert("Your cart is empty!");
    } else {
        alert("Thank you for your order! Total items: " + cart);
        cart = 0;
        document.getElementById("cart-count").innerText = cart;
    }
}

function signup(){
    let user = document.getElementById("newUser").value;
    let pass = document.getElementById("newPass").value;

    if(user === "" || pass === ""){
        alert("Please fill all fields");
        return;
    }

    localStorage.setItem("user", user);
    localStorage.setItem("pass", pass);

    alert("Account created successfully!");
    window.location.href = "login.html";
}

function login(){
    let user = document.getElementById("loginUser").value;
    let pass = document.getElementById("loginPass").value;

    let savedUser = localStorage.getItem("user");
    let savedPass = localStorage.getItem("pass");

    if(user === savedUser && pass === savedPass){
        alert("Login successful!");
        window.location.href = "index.html";
    } else {
        alert("Wrong username or password");
    }
}
function loadDashboard(){

    let users = localStorage.getItem("user") ? 1 : 0;

    let sales = Math.floor(Math.random() * 5000);

    document.getElementById("users").innerText = users;
    document.getElementById("sales").innerText = sales;

    alert("Dashboard Loaded!");
}

let cart = [];

function addToCart(name, price){
    cart.push({name, price});
    alert(name + " added to cart");
}

function viewCart(){
    let total = 0;
    let list = "Your Cart:\n\n";

    cart.forEach(item => {
        list += item.name + " - GH₵" + item.price + "\n";
        total += item.price;
    });

    list += "\nTotal: GH₵" + total;

    alert(list);
}

function checkout(){
    if(cart.length === 0){
        alert("Cart is empty!");
        return;
    }

    alert("Order placed successfully!");
    cart = [];
}

function toggleDarkMode(){
    document.body.classList.toggle("dark-mode");
}


























